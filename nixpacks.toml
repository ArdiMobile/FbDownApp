[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install --break-system-packages flask yt-dlp gunicorn"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT} --workers 1 --timeout 300"